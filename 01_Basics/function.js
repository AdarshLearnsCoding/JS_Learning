function addValue(a, b) {
    if (typeof a === 'number' && typeof b === 'number') {
        console.log( a + b)
    } else {
        console.log("incorrect parameter data types")
    }
}

addValue(2, 3)

